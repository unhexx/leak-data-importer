# SOLVER LOOP — Практический паттерн для agentless разработки

> Основной рабочий цикл при прямой работе с моделью (без многоролевой машины).
> Применяется внутри одной сессии Grok CLI, Cursor, Blackbox и т.п. на leak-data-importer.

## Пять шагов (повторять кластерами)

### 1. Inspect (осмотр)
- Прочитай **все** релевантные файлы перед любыми правками.
- Для задач по импорту/нормализации обязательно:
  - `src/leak_data_importer/parsers/normalization.py` (СНИЛС, телефоны, паспорта, имена, даты)
  - `src/leak_data_importer/importers/txt_report.py` и `parsers/dossier_parser.py`
  - `src/leak_data_importer/graph/` (entity, relationships, factories)
  - Соответствующие тесты в `tests/`
  - Фикстуры: `tests/fixtures/`
- Используй grep + read_file (с offset/limit для больших файлов). Не ленись читать контекст.
- Если задача касается БД/миграций — посмотри `db/schema.sql`, `migrations/`, `src/leak_data_importer/db/`.
- Если Neo4j — `src/leak_data_importer/exporters/neo4j_exporter.py`.

**Выход шага**: у тебя в голове есть точная модель того, как сейчас работает кусок системы (и где именно можно безопасно сделать срез).

### 2. Define success (определение успеха)
Запиши в черновик (или прямо в сообщение):
- Какой конкретный тест должен пройти (`test_normalization.py::test_snils_edge_cases` и т.п.)?
- Какой результат должна дать нормализация для конкретного грязного значения из фикстуры?
- Какой узел/связь должен появиться в графе после импорта тестовой записи?
- Какой CLI-команда или Streamlit-страница должна показать ожидаемое поведение?

Если не можешь сформулировать measurable исход — задача ещё не готова к реализации. Разбей крупнее.

### 3. Smallest vertical slice (минимальный вертикальный срез)
- Делай **только** то, что необходимо для прохождения критерия успеха.
- Один файл изменений = идеально. Два-три — уже много.
- Добавляй/обновляй тесты сразу, если это часть среза (не откладывай).
- Коммить после каждого доказанного среза (естественный русский, без упоминания моделей).

Примеры хороших срезов для leak-data-importer:
- Улучшил нормализацию СНИЛС/паспорта в `normalization.py` + добавил 2 edge-case теста на реальных грязных примерах из фикстур.
- Починил парсинг блоков в `txt_report.py` для отчётов с кривыми заголовками + обновил фикстуру.
- Добавил поддержку нового типа связи `PersonLink` в граф + минимальный экспорт в Neo4j + smoke-тест.
- Расширил `report_mapper.py` на новый тип документа + соответствующий тест.

### 4. Proportional verification (пропорциональная проверка)
Не ограничивайся `pytest`. Обязательно:

- Ручной smoke:
  - `python -m pytest tests/ -q --tb=line -k "normalization or import or graph"`
  - Запуск CLI (`python -m src.leak_data_importer.cli ...`) на фикстурах.
  - При необходимости — временный запуск Streamlit + проверка визуализации (только на синтетике!).
- Прочитай итоговые логи / вывод (особенно ошибки нормализации и entity resolution).
- Убедись, что в коде/комментариях/коммитах **нет** упоминаний AI, LLM, Grok, агентов и т.п.
- Для графовых изменений — простая проверка через Neo4j Browser (если локально поднят) или unit-тесты на factories/relationships.

Если срез маленький — проверка тоже небольшая, но **обязательная**. Никогда не коммить без зелёных тестов на изменённой функциональности.

### 5. Reflect (рефлексия)
Ответь себе (в тексте, перед следующим кластером инструментов):

- Что сработало с первого раза?
- Где я потратил лишние ходы (прочитал не то, начал править слишком рано)?
- Что нужно было прочитать раньше (например, все места создания PersonLink)?
- Есть ли новый постоянный урок для `PROJECT_CONTEXT.md`, `SPRINTPLAN.md` или `SELF_IMPROVEMENT_LOG.md`?
- Стоит ли откатить последний коммит (`git reset --soft HEAD~1`)?

Только после рефлексии — переходи к следующей задаче или расширяй текущую.

## Ограничение «3 инструмента»

Внутри одного кластера (до рефлексии) — **максимум 3 вызова инструментов** (read_file, grep, search_replace, run_terminal_command, всё считается).

Это заставляет быть дисциплинированным: читать больше, чем писать.

Если нужно больше 3 — сделай рефлексию раньше и начни новый кластер.

## Примеры рефлексии (хорошие, в стиле проекта)

> "Прочитал normalization.py:142–178 и test_redaction.py. Понял, что валидация СНИЛС сейчас не отсекает мусорные контрольные суммы. Успех определил как: normalize_snils('123-456-789 01') возвращает None + предупреждение в лог. Срез: добавил 6 строк в normalization + 1 тест. Проверка: pytest прошёл, ручная прогонка на фикстуре дала ожидаемый None. Рефлексия: в следующий раз сначала смотреть все места вызова нормализации (importers + linkers), а не только сам модуль."

> "Inspect показал, что dossier_parser.py и txt_report.py дублируют логику извлечения ФИО. Define success: после изменений оба парсера используют общую normalize_person_name. Срез: вынес функцию + 2 теста + минимальный рефакторинг вызывающего кода. Коммит: 'выделил общую нормализацию ФИО, убрал дублирование'. Reflect: хорошо, что не полез в большой рефакторинг всего парсера сразу."

## Когда прерывать цикл и звать architecture review / переходить в agentic

- После 3+ неудачных попыток пройти один и тот же тест/критерий.
- Когда изменения затрагивают схему БД, модель графа, PII/redaction, публичный CLI/Streamlit API.
- Перед merge в main для нетривиальных фич (минимум 1 ревью человеком или сильной моделью в режиме Reviewer через agentic).
- Когда задача разрослась > 3–4 вертикальных срезов — разбей и/или переключись на agentic_loop_template.

## Инструменты, которые особенно хорошо работают в agentless (Grok CLI)

- Точное чтение + grep с контекстом (`-B`/`-A`).
- `search_replace` для точечных правок (избегай write всего файла).
- `run_terminal_command` для `pytest -q --tb=line`, smoke-скриптов, `ruff check`.
- `monitor` + background для долгого Neo4j bulk или Streamlit.
- План-режим (`enter_plan_mode`) перед крупными срезами.

Избегай: массовых рефакторингов без предварительных срезов, "давай перепишем весь importer", правок на реальных данных.

---

Применяй дисциплинированно. Качество рефлексии и размер среза важнее скорости. Главная цель — чистый, проверенный, по-человечески написанный код на русском.